---

---

<section class="banner" id="news" >
    <div class="banner__container">
        <div class="banner__card">
            <h3><i class="ri-battery-2-charge-line"></i>Long Battery Life</h3>
            <p>Game longer without frequent charging thanks to an extended battery life that keeps the controller powered for hours</p>    
        </div>
        <div class="banner__card--secondary">
            <h3><i class="ri-wireless-charging-line"></i>Charge display</h3>
            <p>Stay informed with an integrated charge display that shows battery level, ensuring you never run out of power</p>    
        </div>
    </div>
</section>

<style>
    .banner{
        background-color: #e1e8e1;
        background-image: url(../assets/specs.png);
        background-position: top center;
        background-size: cover;
        background-repeat: no-repeat;
        padding: 5rem 1rem;
    }

    .banner__container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-template-rows: repeat(2, 1fr);
    grid-column-gap: 0px;
    grid-row-gap: 0px;
    max-width: var(--max-width);
    margin: 0 auto;
    gap: 15rem;
}
.banner__container h3{
    display: flex;
    gap: 10px;
    align-items: center;
    color: var(--text-dark);
}
.banner__container i{
    font-size: 1.5rem;
}
.banner__card {
     grid-area: 2 / 1 / 3 / 2; 
    padding: 1rem 2rem;
    background-color: var(--white);
    z-index: 1;
    border-radius: 2rem;
    }
.banner__card--secondary {
    padding: 1rem 2rem;
    background-color: var(--white);
    z-index: 1;
    grid-area: 1 / 2 / 2 / 3; 
    border-radius: 2rem;
    }
    .banner__container p{
        color: var(--text-light);
        margin-top: 1rem;
        line-height: 1.5;
        font-size: 1.1rem;
    }

    @media (width < 768px){
    .banner__container {
       display: flex;
       flex-direction: column;
       gap: 2rem;
    }
    }
</style>